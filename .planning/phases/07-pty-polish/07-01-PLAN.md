---
phase: 07-pty-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/interactive.ts
  - src/types.ts
  - src/config.ts
  - tests/interactive.test.ts
autonomous: true
requirements:
  - PTY-01

must_haves:
  truths:
    - "isInteractiveCommand returns true for vim, ssh, less, htop, nano, top, tmux, screen, fzf and all D-07 commands"
    - "isInteractiveCommand returns false when command contains pipes"
    - "isInteractiveCommand returns true for user-configured extra commands"
    - "executeInteractive spawns with stdio: 'inherit' and returns CommandResult"
    - "ShellState has interactiveRunning boolean field"
    - "ClaudeShellConfig has interactive_commands optional array field"
  artifacts:
    - path: "src/interactive.ts"
      provides: "Interactive command detection and execution"
      exports: ["isInteractiveCommand", "executeInteractive"]
    - path: "tests/interactive.test.ts"
      provides: "Unit tests for interactive module"
      min_lines: 30
  key_links:
    - from: "src/interactive.ts"
      to: "src/passthrough.ts"
      via: "shares CommandResult type"
      pattern: "import.*CommandResult.*passthrough"
---

<objective>
Create the interactive command detection and execution module for PTY passthrough.

Purpose: Foundation for making interactive terminal programs (vim, ssh, less, htop) work correctly in ClaudeShell. This module detects known-interactive commands and spawns them with full stdio:inherit so the child process gets direct terminal control.

Output: `src/interactive.ts` with detection + execution functions, updated types/config, and unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pty-polish/07-CONTEXT.md
@.planning/phases/07-pty-polish/07-RESEARCH.md

@src/passthrough.ts
@src/types.ts
@src/config.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/passthrough.ts:
```typescript
export interface CommandResult {
  readonly exitCode: number
  readonly stderr: string
}
```

From src/types.ts:
```typescript
export interface ShellState {
  readonly cdState: CdState
  readonly running: boolean
  readonly lastError: LastError | undefined
  readonly aiStreaming: boolean
  readonly sessionId: string | undefined
  readonly chatMode: boolean
  readonly currentModel: string | undefined
  readonly sessionCost: CostAccumulator
  readonly lastSuggestedFix: string | undefined
  readonly projectContext: import('./context.js').ProjectContext | null
  readonly permissionMode: ClaudeShellPermission
}
```

From src/config.ts:
```typescript
export interface ClaudeShellConfig {
  readonly api_key?: string
  readonly model?: string
  readonly history_size?: number
  readonly prompt_template?: string
  readonly prefix?: string
  readonly permissions?: 'auto' | 'ask' | 'deny'
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create interactive.ts module with detection and execution</name>
  <files>src/interactive.ts, tests/interactive.test.ts</files>
  <read_first>src/passthrough.ts, src/types.ts, tests/passthrough.test.ts</read_first>
  <behavior>
    - isInteractiveCommand('vim file.txt') returns true
    - isInteractiveCommand('vi') returns true
    - isInteractiveCommand('nvim') returns true
    - isInteractiveCommand('nano readme.md') returns true
    - isInteractiveCommand('less log.txt') returns true
    - isInteractiveCommand('more file') returns true
    - isInteractiveCommand('man ls') returns true
    - isInteractiveCommand('ssh host') returns true
    - isInteractiveCommand('htop') returns true
    - isInteractiveCommand('top') returns true
    - isInteractiveCommand('tmux') returns true
    - isInteractiveCommand('screen') returns true
    - isInteractiveCommand('fzf') returns true
    - isInteractiveCommand('cat file | less') returns false (per D-09: pipes not interactive)
    - isInteractiveCommand('echo hello | vim -') returns false (pipe present)
    - isInteractiveCommand('ls') returns false (not in list)
    - isInteractiveCommand('git log') returns false
    - isInteractiveCommand('') returns false
    - isInteractiveCommand('vim file.txt', ['python3']) returns true (still matches default)
    - isInteractiveCommand('python3', ['python3']) returns true (user-configured)
    - isInteractiveCommand('python3') returns false (not in default list)
    - executeInteractive('echo hello') resolves with exitCode 0
    - executeInteractive('exit 42') resolves with exitCode 42
  </behavior>
  <action>
    Create src/interactive.ts per D-04, D-07, D-08, D-09:

    1. Import `spawn` from `node:child_process` and `CommandResult` type from `./passthrough.js`

    2. Define `DEFAULT_INTERACTIVE` as a `ReadonlySet<string>` containing exactly (per D-07):
       `vim`, `vi`, `nvim`, `nano`, `emacs`, `less`, `more`, `man`, `top`, `htop`, `btop`, `ssh`, `telnet`, `tmux`, `screen`, `fzf`

    3. Export `isInteractiveCommand(command: string, userList: readonly string[] = []): boolean`:
       - Per D-09: if `command.includes('|')` return false
       - Per D-08: extract first word via `command.trim().split(/\s/)[0]`
       - Return true if first word is in DEFAULT_INTERACTIVE or in userList
       - Return false for empty string

    4. Export `executeInteractive(command: string, cwd?: string): Promise<CommandResult>`:
       - Per D-01: spawn('bash', ['-c', command], { stdio: 'inherit', cwd: cwd ?? process.cwd(), env: process.env })
       - On 'close': resolve with { exitCode: code ?? 1, stderr: '' }
       - On 'error': resolve with { exitCode: 127, stderr: err.message }
       - Note: stderr is always '' because all stdio is inherited (no capture)

    Create tests/interactive.test.ts following existing test patterns (see tests/passthrough.test.ts):
    - Write all behavior tests listed above
    - Use `describe`/`it`/`expect` from vitest
    - For executeInteractive tests, use simple commands (echo, exit) that work without a TTY
  </action>
  <acceptance_criteria>
    - grep -q "isInteractiveCommand" src/interactive.ts
    - grep -q "executeInteractive" src/interactive.ts
    - grep -q "DEFAULT_INTERACTIVE" src/interactive.ts
    - grep -q "stdio: 'inherit'" src/interactive.ts
    - grep -q "includes('|')" src/interactive.ts
    - grep -q "isInteractiveCommand" tests/interactive.test.ts
    - grep -q "executeInteractive" tests/interactive.test.ts
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tald/Projects/claudeshell && npx vitest run tests/interactive.test.ts</automated>
  </verify>
  <done>All behavior tests pass. isInteractiveCommand correctly detects D-07 list commands, rejects piped commands per D-09, and supports user-extensible list per D-11. executeInteractive spawns with stdio:'inherit' per D-01.</done>
</task>

<task type="auto">
  <name>Task 2: Add interactiveRunning to ShellState and interactive_commands to Config</name>
  <files>src/types.ts, src/config.ts</files>
  <read_first>src/types.ts, src/config.ts</read_first>
  <action>
    Update src/types.ts per D-14:
    - Add `readonly interactiveRunning: boolean` field to ShellState interface (after `aiStreaming`)

    Update src/config.ts per D-11:
    - Add `readonly interactive_commands?: readonly string[]` field to ClaudeShellConfig interface
    - In `loadConfig()`, add parsing: if `Array.isArray(obj.interactive_commands)` and every element is a string, include it in the config object. Use: `...(Array.isArray(obj.interactive_commands) && obj.interactive_commands.every((x: unknown) => typeof x === 'string') ? { interactive_commands: obj.interactive_commands as readonly string[] } : {})`
    - In `loadProjectConfig()`, add the same parsing for the interactive_commands field

    Do NOT modify shell.ts in this task -- that is Plan 02.
  </action>
  <acceptance_criteria>
    - grep -q "interactiveRunning" src/types.ts
    - grep -q "interactive_commands" src/config.ts
    - grep -q "interactive_commands" src/types.ts || grep -q "interactive_commands" src/config.ts
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tald/Projects/claudeshell && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>ShellState has interactiveRunning boolean. ClaudeShellConfig has interactive_commands optional array. TypeScript compiles cleanly. All existing tests still pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx vitest run` passes all tests including new interactive.test.ts
- `grep -q "isInteractiveCommand" src/interactive.ts` succeeds
- `grep -q "executeInteractive" src/interactive.ts` succeeds
- `grep -q "interactiveRunning" src/types.ts` succeeds
- `grep -q "interactive_commands" src/config.ts` succeeds
</verification>

<success_criteria>
- interactive.ts exports isInteractiveCommand and executeInteractive
- Detection covers all D-07 commands, rejects pipes per D-09, supports user list per D-11
- executeInteractive spawns with stdio:'inherit' per D-01
- ShellState has interactiveRunning field per D-14
- Config supports interactive_commands array per D-11
- All tests pass, TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-pty-polish/07-01-SUMMARY.md`
</output>
