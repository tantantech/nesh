---
phase: 03-distribution-platform
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/ci-test.sh
  - tests/shell.integration.test.ts
autonomous: true
requirements: [PLAT-01, PLAT-02]

must_haves:
  truths:
    - "All path operations use os.homedir() and path.join() (no hardcoded / or ~)"
    - "Build output runs correctly on the current platform"
    - "CI test script validates build output, shebang, executable permission, and package contents"
    - "Integration tests verify the built CLI works end-to-end"
  artifacts:
    - path: "scripts/ci-test.sh"
      provides: "CI validation script for Linux containers"
      contains: "dist/cli.js"
    - path: "tests/shell.integration.test.ts"
      provides: "Integration tests covering built artifact"
  key_links:
    - from: "scripts/ci-test.sh"
      to: "dist/cli.js"
      via: "build output verification"
      pattern: "test -f dist/cli.js"
---

<objective>
Validate cross-platform compatibility (macOS + Linux) and create a CI test script for Linux container validation.

Purpose: PLAT-01 (macOS) is already complete from Phase 1 but needs re-verification after Phase 3 changes. PLAT-02 (Linux) requires an audit of all path operations and a CI script that can run in Linux containers.
Output: CI test script, path audit confirmation, verified build on current platform.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-distribution-platform/03-CONTEXT.md
@.planning/phases/03-distribution-platform/03-RESEARCH.md
@.planning/phases/03-distribution-platform/03-01-SUMMARY.md

@src/config.ts
@src/history.ts
@src/prompt.ts
@src/passthrough.ts
@src/shell.ts
@src/cli.ts
@package.json
@tests/shell.integration.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit path operations and create CI test script</name>
  <files>scripts/ci-test.sh</files>
  <read_first>src/config.ts, src/history.ts, src/prompt.ts, src/passthrough.ts, src/shell.ts, src/builtins.ts</read_first>
  <action>
    1. Audit all src/*.ts files for path operations per D-15, D-16, D-17, D-18:
       - Grep for hardcoded paths: `/Users/`, `/home/`, `~` used as literal string (not in comments)
       - Verify all file paths use `path.join()` or `path.resolve()` with `os.homedir()` or `process.cwd()`
       - Verify `process.env.HOME` is never used directly (use `os.homedir()` instead)
       - Verify bash spawn uses `'bash'` (available on both macOS and Linux per D-17)
       - Document audit results in the summary
       - If any issues found, fix them
    2. Create scripts/ci-test.sh per D-19:
       - `#!/usr/bin/env bash` with `set -euo pipefail`
       - Run `npm ci` (clean install)
       - Run `npm run build`
       - Run `npm run test`
       - Verify `dist/cli.js` exists
       - Verify first line contains shebang `#!/usr/bin/env node`
       - Verify file is executable (`test -x dist/cli.js`)
       - Verify `npm pack --dry-run` includes `dist/cli.js`
       - Verify `node dist/cli.js --version` outputs version string
       - Print "All CI checks passed" on success
       - Make script executable: `chmod +x scripts/ci-test.sh`
    3. Run the CI script locally to verify it passes on macOS (PLAT-01 re-verification).
  </action>
  <verify>
    <automated>cd /Users/tald/Projects/claudeshell && bash scripts/ci-test.sh</automated>
  </verify>
  <acceptance_criteria>
    - test -f scripts/ci-test.sh
    - test -x scripts/ci-test.sh
    - grep -q "dist/cli.js" scripts/ci-test.sh
    - grep -q "shebang" scripts/ci-test.sh || grep -q "usr/bin/env node" scripts/ci-test.sh
    - bash scripts/ci-test.sh exits 0
  </acceptance_criteria>
  <done>All src/ path operations confirmed OS-agnostic. CI test script exists, is executable, validates build output + shebang + permissions + package contents, and passes on macOS.</done>
</task>

<task type="auto">
  <name>Task 2: Update integration tests for built artifact</name>
  <files>tests/shell.integration.test.ts</files>
  <read_first>tests/shell.integration.test.ts</read_first>
  <action>
    1. Read existing integration tests to understand the spawn-based testing pattern.
    2. Add a test case that runs the built artifact (`node dist/cli.js`) instead of the dev entry (`tsx src/cli.ts`):
       - Spawn `node dist/cli.js --version` and verify it outputs the version string
       - This confirms the build output is functional end-to-end
    3. Add a test case that verifies `node dist/cli.js` starts and responds to `exit` command:
       - Spawn the process, write "exit\n" to stdin, verify clean exit (code 0)
       - Use a timeout of 5 seconds to prevent hanging
    4. Run the full test suite to confirm all tests pass.
  </action>
  <verify>
    <automated>cd /Users/tald/Projects/claudeshell && npm run build && npx vitest run tests/shell.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "dist/cli.js" tests/shell.integration.test.ts
    - grep -q "version" tests/shell.integration.test.ts
    - npx vitest run tests/shell.integration.test.ts exits 0
  </acceptance_criteria>
  <done>Integration tests cover built artifact: --version flag works, shell starts and exits cleanly from dist/cli.js. All tests pass.</done>
</task>

</tasks>

<verification>
1. `bash scripts/ci-test.sh` passes all checks
2. `npm test` -- all tests pass including new integration tests
3. Path audit confirms no hardcoded macOS paths in src/
4. Built artifact works: `node dist/cli.js --version` and `echo exit | node dist/cli.js`
</verification>

<success_criteria>
- PLAT-01: All Phase 1+2+3 functionality verified on macOS via CI script and integration tests
- PLAT-02: Path audit confirms Linux compatibility; CI script designed for Linux containers
- Integration tests cover built artifact (dist/cli.js), not just dev entry (tsx src/cli.ts)
</success_criteria>

<output>
After completion, create `.planning/phases/03-distribution-platform/03-02-SUMMARY.md`
</output>
