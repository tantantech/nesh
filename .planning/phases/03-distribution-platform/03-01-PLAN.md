---
phase: 03-distribution-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/cli.ts
  - src/ai.ts
  - package.json
  - tests/config.test.ts
autonomous: true
requirements: [CONF-03, PLAT-03]

must_haves:
  truths:
    - "User can configure api_key, model, and history_size via ~/.claudeshell/config.json"
    - "Config file is optional -- shell works without it using env vars and defaults"
    - "Invalid JSON in config file warns on stderr and uses defaults (no crash)"
    - "npm pack produces a tarball containing only dist/ and package.json"
    - "dist/cli.js exists with shebang after build"
    - "claudeshell --version prints the version"
  artifacts:
    - path: "src/config.ts"
      provides: "Full config loader with ClaudeShellConfig interface"
      exports: ["ClaudeShellConfig", "loadConfig", "resolveApiKey", "ensureConfigDir"]
      contains: "config.json"
    - path: "package.json"
      provides: "npm distribution metadata"
      contains: "prepublishOnly"
    - path: "dist/cli.js"
      provides: "Built CLI with shebang"
      contains: "#!/usr/bin/env node"
  key_links:
    - from: "src/ai.ts"
      to: "src/config.ts"
      via: "loadConfig() + resolveApiKey(config)"
      pattern: "resolveApiKey\\(.*loadConfig"
    - from: "package.json"
      to: "dist/cli.js"
      via: "bin field"
      pattern: '"claudeshell".*dist/cli.js'
---

<objective>
Extend the config module to support a full config file at ~/.claudeshell/config.json and configure package.json for npm global installation.

Purpose: CONF-03 (config file) and PLAT-03 (npm installable) are the two production-readiness requirements. This plan makes ClaudeShell configurable and distributable.
Output: Extended config.ts, updated package.json with bin/files/build/prepublishOnly, --version flag in cli.ts, passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-distribution-platform/03-CONTEXT.md
@.planning/phases/03-distribution-platform/03-RESEARCH.md

@src/config.ts
@src/cli.ts
@src/ai.ts
@src/shell.ts
@src/history.ts
@tests/config.test.ts
@package.json
@tsconfig.json

<interfaces>
<!-- Current config.ts exports (to be replaced) -->
From src/config.ts:
```typescript
export function resolveApiKey(): string | undefined
```

From src/ai.ts (consumer of config):
```typescript
// Line 107: const apiKey = resolveApiKey()
import { resolveApiKey } from './config.js'
```

From src/shell.ts (will consume history_size from config):
```typescript
// Line 14: historySize: 10_000 (hardcoded -- should come from config)
const rl = readline.createInterface({
  historySize: 10_000,
})
```

From src/history.ts:
```typescript
export const MAX_HISTORY = 10_000  // hardcoded -- could come from config
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend config module with full config schema and update consumers</name>
  <files>src/config.ts, src/ai.ts, src/shell.ts, src/history.ts, tests/config.test.ts</files>
  <read_first>src/config.ts, src/ai.ts, src/shell.ts, src/history.ts, tests/config.test.ts</read_first>
  <behavior>
    - loadConfig() with no config file returns defaults: { history_size: 1000 }
    - loadConfig() with valid JSON parses api_key, model, history_size fields
    - loadConfig() with invalid JSON writes warning to stderr and returns defaults
    - loadConfig() ignores unknown fields in config JSON (type narrowing)
    - resolveApiKey(config) returns env var ANTHROPIC_API_KEY over config.api_key
    - resolveApiKey(config) returns config.api_key when env var is unset
    - ensureConfigDir() creates ~/.claudeshell/ directory if missing
  </behavior>
  <action>
    1. Rewrite src/config.ts per D-09, D-10, D-11, D-12, D-13, D-14:
       - Export `interface ClaudeShellConfig { readonly api_key?: string; readonly model?: string; readonly history_size?: number }`
       - `CONFIG_DIR = path.join(os.homedir(), '.claudeshell')`
       - `CONFIG_PATH = path.join(CONFIG_DIR, 'config.json')` (per D-09, JSON format)
       - `DEFAULTS: ClaudeShellConfig = { history_size: 1000 }`
       - `loadConfig(): ClaudeShellConfig` -- reads CONFIG_PATH, parses JSON, type-narrows each field individually (typeof checks), merges with DEFAULTS via spread. On ENOENT: silently return defaults. On parse error: write warning to stderr via `process.stderr.write()` (not console.log), return defaults (per D-14).
       - `resolveApiKey(config?: ClaudeShellConfig): string | undefined` -- env var first, then config.api_key
       - `ensureConfigDir(): void` -- `fs.mkdirSync(CONFIG_DIR, { recursive: true })` (per D-12)
    2. Update src/ai.ts: import `loadConfig` alongside `resolveApiKey`. At line 107, change to: `const config = loadConfig(); const apiKey = resolveApiKey(config)`. This wires config into AI usage.
    3. Update src/shell.ts: import `loadConfig` from config.js. Call `const config = loadConfig()` at top of `runShell()`. Pass `config.history_size ?? 10_000` to `readline.createInterface({ historySize })`. This wires CONF-03 into shell behavior.
    4. Update tests/config.test.ts: rewrite tests for new signatures. Test loadConfig() with mocked fs for: no file, valid JSON, invalid JSON (check stderr.write called), partial fields. Test resolveApiKey(config) with/without env var. Keep vi.mock('node:fs') pattern.
  </action>
  <verify>
    <automated>cd /Users/tald/Projects/claudeshell && npx vitest run tests/config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ClaudeShellConfig" src/config.ts
    - grep -q "config.json" src/config.ts
    - grep -q "loadConfig" src/ai.ts
    - grep -q "loadConfig" src/shell.ts
    - grep -q "history_size" src/config.ts
    - npx vitest run tests/config.test.ts exits 0
  </acceptance_criteria>
  <done>loadConfig() returns typed config from ~/.claudeshell/config.json with defaults fallback. resolveApiKey(config) prioritizes env var. ai.ts and shell.ts consume the new config. All config tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Configure package.json for npm distribution and add --version flag</name>
  <files>package.json, src/cli.ts</files>
  <read_first>package.json, src/cli.ts, src/ai.ts</read_first>
  <action>
    1. Update package.json per D-01 through D-08:
       - Verify `"name": "claudeshell"` (per D-01, already set)
       - Verify `"bin": { "claudeshell": "dist/cli.js" }` (per D-02, already set)
       - Update `"build"` script to: `"tsdown src/cli.ts --format esm --out-dir dist"` -- then test if output is `dist/cli.js` or `dist/cli.mjs`. If `.mjs`, add `--out-extension .js` flag. The research warns about this mismatch (Pitfall 1). (per D-03, D-05, D-08)
       - Add `"files": ["dist"]` (per D-04)
       - Add `"prepublishOnly": "npm run build && npm run test"` (per D-06)
       - Verify `"type": "module"` present (per D-07, already set)
    2. Add --version flag to src/cli.ts:
       - Before `runShell()` call, check `process.argv.includes('--version') || process.argv.includes('-v')`
       - If true, use `import { createRequire } from 'node:module'` to read `../package.json` version field, print `claudeshell v${version}`, and `process.exit(0)`
       - If createRequire approach fails during build verification, switch to tsdown `--define` approach per research Pitfall 6
    3. Run `npm run build` and verify:
       - `dist/cli.js` exists (not just `dist/cli.mjs`)
       - First line is `#!/usr/bin/env node`
       - File is executable (`ls -la dist/cli.js`)
       - File size is reasonable (under 50KB, not bundling node_modules)
    4. Run `npm pack --dry-run` and verify only dist/ files and package.json are included (per Pitfall 4)
    5. Run full test suite: `npm test`
  </action>
  <verify>
    <automated>cd /Users/tald/Projects/claudeshell && npm run build && test -f dist/cli.js && head -1 dist/cli.js | grep -q "#!/usr/bin/env node" && npm pack --dry-run 2>&1 | grep -q "dist/cli.js" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - grep -q '"files"' package.json
    - grep -q 'prepublishOnly' package.json
    - grep -q 'version' src/cli.ts
    - test -f dist/cli.js
    - head -1 dist/cli.js contains shebang
    - npm test exits 0
  </acceptance_criteria>
  <done>package.json has bin, files, build, prepublishOnly fields. Build produces dist/cli.js with shebang. npm pack --dry-run shows only dist/ contents. --version flag prints version. All tests pass.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds and produces dist/cli.js with shebang
2. `npm test` -- all tests pass
3. `npm pack --dry-run` -- shows only dist/ files
4. `node dist/cli.js --version` -- prints version
5. Config file at ~/.claudeshell/config.json is read if present, ignored if absent
</verification>

<success_criteria>
- CONF-03: config.ts exports loadConfig() reading ~/.claudeshell/config.json with api_key, model, history_size fields
- PLAT-03: package.json has bin, files, prepublishOnly; build produces dist/cli.js with shebang; npm pack includes only dist/
- All existing tests continue to pass
- --version flag works
</success_criteria>

<output>
After completion, create `.planning/phases/03-distribution-platform/03-01-SUMMARY.md`
</output>
