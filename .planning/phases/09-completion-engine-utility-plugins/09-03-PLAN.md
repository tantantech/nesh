---
phase: 09-completion-engine-utility-plugins
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/plugins/completions/git-completions.ts
  - src/plugins/completions/docker-completions.ts
  - src/plugins/completions/npm-completions.ts
  - src/plugins/completions/kubectl-completions.ts
  - src/plugins/completions/cloud-completions.ts
  - src/plugins/completions/devtools-completions.ts
  - src/plugins/completions/sysadmin-completions.ts
  - src/plugins/utilities/extract.ts
  - src/plugins/utilities/sudo.ts
  - src/plugins/utilities/copypath.ts
  - src/plugins/utilities/encode64.ts
  - src/plugins/utilities/urltools.ts
  - src/plugins/utilities/jsontools.ts
  - src/plugins/utilities/web-search.ts
  - src/plugins/utilities/dirhistory.ts
  - src/plugins/index.ts
  - tests/completions/git-completions.test.ts
  - tests/plugins/utilities.test.ts
autonomous: true
requirements:
  - COMP-05
  - PORT-03
  - PORT-04

must_haves:
  truths:
    - "User can press Tab after git ch and see checkout/cherry-pick completions"
    - "Top 20 commands (git, docker, npm, yarn, pnpm, kubectl, ssh, aws, gcloud, az, terraform, helm, cargo, pip, python, node, make, systemctl, brew, apt) have hand-crafted completions"
    - "Utility plugins (extract, sudo, copypath, encode64, urltools, jsontools, web-search, dirhistory) are bundled and work cross-platform"
    - "All completion and utility plugins are registered in BUNDLED_PLUGINS"
  artifacts:
    - path: "src/plugins/completions/git-completions.ts"
      provides: "Git completion spec with branch/remote generators"
      exports: ["plugin"]
    - path: "src/plugins/completions/docker-completions.ts"
      provides: "Docker completion spec"
      exports: ["plugin"]
    - path: "src/plugins/completions/npm-completions.ts"
      provides: "npm/yarn/pnpm completion specs"
      exports: ["plugin"]
    - path: "src/plugins/utilities/extract.ts"
      provides: "Archive extraction command plugin"
      exports: ["plugin"]
    - path: "src/plugins/utilities/sudo.ts"
      provides: "Sudo toggle keybinding plugin"
      exports: ["plugin"]
    - path: "src/plugins/utilities/copypath.ts"
      provides: "Copy cwd to clipboard plugin"
      exports: ["plugin"]
    - path: "src/plugins/index.ts"
      provides: "All bundled plugins array"
      contains: "BUNDLED_PLUGINS"
  key_links:
    - from: "src/plugins/completions/git-completions.ts"
      to: "src/completions/types.ts"
      via: "imports CompletionSpec, CompletionGenerator"
      pattern: "import.*CompletionSpec.*from"
    - from: "src/plugins/index.ts"
      to: "src/plugins/completions/"
      via: "imports all completion plugins"
      pattern: "import.*completions"
    - from: "src/plugins/index.ts"
      to: "src/plugins/utilities/"
      via: "imports all utility plugins"
      pattern: "import.*utilities"
---

<objective>
Create hand-crafted completion specs for the top 20 commands and port ~8 core utility plugins to TypeScript.

Purpose: This delivers the user-facing value -- Tab completions for the most common developer tools, plus utility plugins like extract, sudo toggle, and copypath that "just work" after enabling.
Output: 7 completion plugin files covering 20 commands, 8 utility plugin files, all registered in BUNDLED_PLUGINS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-completion-engine-utility-plugins/09-CONTEXT.md
@.planning/phases/09-completion-engine-utility-plugins/09-RESEARCH.md
@.planning/phases/09-completion-engine-utility-plugins/09-01-SUMMARY.md
@.planning/phases/09-completion-engine-utility-plugins/09-02-SUMMARY.md

@src/completions/types.ts
@src/plugins/types.ts
@src/plugins/index.ts
@src/plugins/git.ts

<interfaces>
<!-- From Plan 01 and Plan 02 outputs -->

From src/completions/types.ts:
```typescript
export interface CompletionSpec {
  readonly name: string
  readonly subcommands?: Readonly<Record<string, CompletionSpec>>
  readonly options?: readonly CompletionOption[]
  readonly args?: readonly CompletionArg[]
}

export interface CompletionOption {
  readonly name: string | readonly string[]
  readonly description?: string
  readonly args?: readonly CompletionArg[]
}

export interface CompletionArg {
  readonly name: string
  readonly template?: 'filepaths' | 'folders'
  readonly generators?: readonly CompletionGenerator[]
}

export type CompletionGenerator = (context: Readonly<CompletionContext>) => Promise<readonly string[]>
```

From src/plugins/types.ts (after Plan 02 extension):
```typescript
export interface PluginManifest {
  readonly name: string
  readonly version: string
  readonly description: string
  readonly aliases?: Readonly<Record<string, string>>
  readonly completions?: CompletionProvider
  readonly completionSpecs?: readonly CompletionSpec[]
  readonly dependencies?: readonly string[]
  readonly platform?: 'macos' | 'linux' | 'all'
  // ...
}
```

From src/plugins/git.ts (alias-only plugin pattern):
```typescript
export const plugin: PluginManifest = {
  name: 'git',
  version: '1.0.0',
  description: 'Git aliases (ported from oh-my-zsh)',
  aliases: { gst: 'git status', ... },
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Top 20 command completion specs</name>
  <files>src/plugins/completions/git-completions.ts, src/plugins/completions/docker-completions.ts, src/plugins/completions/npm-completions.ts, src/plugins/completions/kubectl-completions.ts, src/plugins/completions/cloud-completions.ts, src/plugins/completions/devtools-completions.ts, src/plugins/completions/sysadmin-completions.ts, tests/completions/git-completions.test.ts</files>
  <read_first>src/completions/types.ts, src/plugins/types.ts, src/plugins/git.ts</read_first>
  <behavior>
    - git-completions plugin has completionSpecs with git spec containing subcommands: checkout, branch, merge, rebase, commit, push, pull, fetch, stash, log, diff, add, reset, tag, remote, switch, cherry-pick, revert, bisect, clean
    - git-completions spec has generators for branch names (git branch --list) and remote names (git remote)
    - git-completions spec has filepaths template for `git add` args
    - docker-completions has subcommands: run, build, ps, images, pull, push, exec, logs, stop, start, rm, rmi, compose
    - npm-completions covers npm (install, run, test, start, init, publish, uninstall), yarn (add, remove, run, build, test), pnpm (install, add, remove, run, build, test)
    - kubectl-completions has subcommands: get, describe, apply, delete, logs, exec, port-forward, create, edit, scale, rollout
    - cloud-completions covers aws (s3, ec2, lambda, iam, cloudformation), gcloud (compute, container, functions, iam, projects), az (vm, storage, webapp, group, login)
    - devtools-completions covers cargo (build, run, test, bench, doc, clean, check), pip (install, uninstall, freeze, list, show), python (-m, -c), node (-e, --inspect), make (with filepaths template)
    - sysadmin-completions covers ssh (with known_hosts generator), systemctl (start, stop, restart, status, enable, disable), brew (install, uninstall, update, upgrade, search, list, info), apt (install, remove, update, upgrade, search), terraform (init, plan, apply, destroy, validate, fmt, state), helm (install, upgrade, uninstall, list, repo, search, status)
    - Each plugin file exports `plugin: PluginManifest` with completionSpecs field
  </behavior>
  <action>
    Create 7 completion plugin files in `src/plugins/completions/` per D-23 through D-26. Each exports `plugin: PluginManifest` with a `completionSpecs` field.

    **git-completions.ts** (most detailed -- the reference implementation):
    - Create `gitBranchGenerator: CompletionGenerator` that runs `git branch --list --format='%(refname:short)'` with 1s timeout, splits stdout by newline, filters empty
    - Create `gitRemoteGenerator: CompletionGenerator` that runs `git remote` with 1s timeout
    - Create `gitTagGenerator: CompletionGenerator` that runs `git tag --list` with 1s timeout
    - Build `gitSpec: CompletionSpec` with name 'git' and subcommands for: checkout (args: branch generator, options: -b, -f), branch (args: branch generator, options: -d/-D, -a, -r, -m), merge (args: branch generator, options: --abort, --continue, --squash), rebase (args: branch generator, options: --abort, --continue, --interactive, --onto), commit (options: -m, -a, --amend, --no-edit, -v), push (options: --force, --force-with-lease, -u, --tags), pull (options: --rebase, --ff-only), fetch (options: --all, --prune), stash (subcommands: push, pop, apply, list, drop, show), log (options: --oneline, --graph, --all, --stat, -n), diff (options: --staged, --word-diff, --name-only), add (args: filepaths template, options: --all, --patch, --update), reset (args: branch generator, options: --hard, --soft, --mixed), tag (args: tag generator, options: -a, -d, -m), remote (subcommands: add, remove, rename, show; args: remote generator), switch (args: branch generator, options: -c, --create), cherry-pick (args: branch generator, options: --abort, --continue), revert (options: --abort, --continue), bisect (subcommands: start, bad, good, reset, skip), clean (options: -f, -d, -n, -x)
    - Export: `export const plugin: PluginManifest = { name: 'git-completions', version: '1.0.0', description: 'Git Tab completions', completionSpecs: [gitSpec] }`

    **docker-completions.ts**: Static subcommand specs for docker CLI. Include `dockerContainerGenerator` (runs `docker ps -a --format '{{.Names}}'`) and `dockerImageGenerator` (runs `docker images --format '{{.Repository}}:{{.Tag}}'`). Subcommands: run (args: image generator), build (args: filepaths), ps (options: -a, -q), images (options: -a, -q), pull/push (args: image generator), exec (args: container generator), logs (args: container generator), stop/start/rm (args: container generator), rmi (args: image generator), compose (subcommands: up, down, build, ps, logs, exec).

    **npm-completions.ts**: Three specs (npm, yarn, pnpm). Mostly static subcommands. npm: install (args: filepaths), run (generator: reads package.json scripts), test, start, init, publish, uninstall. yarn: add, remove, run (generator), build, test. pnpm: install, add, remove, run (generator), build, test. The `npmScriptGenerator` reads package.json from cwd and returns script names.

    **kubectl-completions.ts**: Static subcommands. Include `kubectlResourceGenerator` listing common resource types as static array (pods, deployments, services, configmaps, secrets, namespaces, nodes, ingress, pv, pvc, jobs, cronjobs). get/describe/delete take resource type arg then resource name. apply/create with -f filepaths. logs/exec take pod name.

    **cloud-completions.ts**: Three specs (aws, gcloud, az). Mostly static nested subcommands -- no dynamic generators (cloud CLIs have their own completion). aws: s3 (ls, cp, mv, rm, sync), ec2 (describe-instances, run-instances, terminate-instances, start-instances, stop-instances), lambda (invoke, list-functions, create-function, update-function-code), iam (list-users, create-user, create-role), cloudformation (deploy, create-stack, delete-stack, describe-stacks). gcloud: compute (instances, disks, firewall-rules), container (clusters, node-pools), functions (deploy, list, delete, logs), iam (service-accounts), projects (list, describe, create). az: vm (create, list, delete, start, stop), storage (account, container, blob), webapp (create, list, delete, deploy), group (create, list, delete, show), login.

    **devtools-completions.ts**: Specs for cargo, pip, python, node, make. cargo: build, run, test, bench, doc, clean, check (options: --release, --target). pip: install, uninstall, freeze, list, show. python: -m (common modules: venv, http.server, json.tool, pip), -c. node: -e, --inspect, --inspect-brk. make: args with filepaths template.

    **sysadmin-completions.ts**: Specs for ssh, systemctl, brew, apt, terraform, helm. ssh: `sshHostGenerator` reads ~/.ssh/known_hosts and ~/.ssh/config for hostnames. systemctl: start/stop/restart/status/enable/disable (all take unit name arg -- no generator, rely on compgen). brew: install, uninstall, update, upgrade, search, list, info, doctor, cleanup. apt: install, remove, update, upgrade, search, autoremove, purge. terraform: init, plan, apply, destroy, validate, fmt, state (subcommands: list, show, rm, mv). helm: install, upgrade, uninstall, list, repo (subcommands: add, remove, update, list), search (subcommands: repo, hub), status.

    Use `promisify(execFile)` from `node:child_process` for all generators. All generators have try/catch returning empty array on failure.

    Create `tests/completions/git-completions.test.ts`:
    - Test that gitSpec has expected subcommand names
    - Test that gitBranchGenerator returns branches (mock execFile)
    - Test that checkout subcommand has branch generator in args
    - Test that add subcommand has filepaths template
  </action>
  <verify>
    <automated>cd /Users/tald/Projects/nesh/main && npx vitest run tests/completions/git-completions.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "completionSpecs" src/plugins/completions/git-completions.ts` returns >= 1
    - `grep "gitBranchGenerator" src/plugins/completions/git-completions.ts` matches
    - `grep "name: 'git'" src/plugins/completions/git-completions.ts` matches
    - `grep "name: 'docker'" src/plugins/completions/docker-completions.ts` matches
    - `grep "name: 'npm'" src/plugins/completions/npm-completions.ts` matches
    - `grep "name: 'kubectl'" src/plugins/completions/kubectl-completions.ts` matches
    - `grep "name: 'aws'" src/plugins/completions/cloud-completions.ts` matches
    - `grep "name: 'cargo'" src/plugins/completions/devtools-completions.ts` matches
    - `grep "name: 'ssh'" src/plugins/completions/sysadmin-completions.ts` matches
    - 7 files exist in `src/plugins/completions/`
    - `npx vitest run tests/completions/git-completions.test.ts` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 20 commands have Fig-style completion specs with dynamic generators for git branches/remotes, docker containers/images, npm scripts, and ssh hosts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Utility plugins and BUNDLED_PLUGINS registration</name>
  <files>src/plugins/utilities/extract.ts, src/plugins/utilities/sudo.ts, src/plugins/utilities/copypath.ts, src/plugins/utilities/encode64.ts, src/plugins/utilities/urltools.ts, src/plugins/utilities/jsontools.ts, src/plugins/utilities/web-search.ts, src/plugins/utilities/dirhistory.ts, src/plugins/index.ts, tests/plugins/utilities.test.ts</files>
  <read_first>src/plugins/types.ts, src/plugins/git.ts, src/plugins/index.ts</read_first>
  <behavior>
    - extract plugin exports PluginManifest with name 'extract' and aliases: x='extract'
    - extract plugin has EXTRACTORS map: .tar.gz -> tar xzf, .zip -> unzip, .gz -> gunzip, .bz2 -> bunzip2, .xz -> unxz, .tar -> tar xf, .tar.bz2 -> tar xjf, .tar.xz -> tar xJf, .7z -> 7z x, .rar -> unrar x
    - sudo plugin exports PluginManifest with name 'sudo'
    - copypath plugin exports PluginManifest with name 'copypath' and uses pbcopy on macOS, xclip on Linux
    - encode64 plugin exports PluginManifest with name 'encode64' and aliases: e64='encode64', d64='decode64'
    - urltools plugin exports PluginManifest with name 'urltools' and aliases: urlencode, urldecode
    - jsontools plugin exports PluginManifest with name 'jsontools' and aliases: pp_json='json_pp', is_json
    - web-search plugin exports PluginManifest with name 'web-search' and aliases for google, github, stackoverflow
    - dirhistory plugin exports PluginManifest with name 'dirhistory'
    - BUNDLED_PLUGINS includes all completion plugins and utility plugins
  </behavior>
  <action>
    Create 8 utility plugin files in `src/plugins/utilities/` per D-27 through D-33:

    **extract.ts** per D-29:
    - Define `EXTRACTORS: Readonly<Record<string, readonly string[]>>` mapping extensions to commands: '.tar.gz' -> ['tar', 'xzf'], '.tgz' -> ['tar', 'xzf'], '.tar.bz2' -> ['tar', 'xjf'], '.tar.xz' -> ['tar', 'xJf'], '.tar' -> ['tar', 'xf'], '.zip' -> ['unzip'], '.gz' -> ['gunzip'], '.bz2' -> ['bunzip2'], '.xz' -> ['unxz'], '.7z' -> ['7z', 'x'], '.rar' -> ['unrar', 'x']
    - Export `plugin: PluginManifest` with name 'extract', aliases: { x: 'extract' }, and a comment that the actual extract command registration will come in Phase 11 with the plugin command API (D-28). For now it registers the 'x' alias as a convenience.

    **sudo.ts** per D-30:
    - Export `plugin: PluginManifest` with name 'sudo', description 'Toggle sudo prefix on current line'
    - Note: The actual keybinding (Escape+Escape) requires the keypress API from D-28. For now, register alias: `please` -> `sudo` as a convenience. Full keybinding implementation deferred to when registerKeybinding API is available.

    **copypath.ts** per D-31:
    - Export `plugin: PluginManifest` with name 'copypath'
    - Define aliases: `copypath` -> platform-specific command. On macOS: `pwd | pbcopy`; on Linux: `pwd | xclip -selection clipboard`
    - Use `process.platform === 'darwin'` to choose

    **encode64.ts**:
    - Aliases: `e64` -> `base64`, `d64` -> `base64 --decode`

    **urltools.ts**:
    - Aliases: `urlencode` -> `python3 -c "import sys, urllib.parse; print(urllib.parse.quote(sys.argv[1]))"`, `urldecode` -> `python3 -c "import sys, urllib.parse; print(urllib.parse.unquote(sys.argv[1]))"`

    **jsontools.ts**:
    - Aliases: `pp_json` -> `python3 -m json.tool`, `is_json` -> `python3 -c "import json,sys; json.load(sys.stdin); print('Valid JSON')"`

    **web-search.ts**:
    - Aliases: `google` -> `open "https://www.google.com/search?q="` (macOS) or `xdg-open` (Linux). Use platform detection.
    - Also: `github` -> `open "https://github.com/search?q="`, `stackoverflow` -> `open "https://stackoverflow.com/search?q="`

    **dirhistory.ts**:
    - Aliases: `d` -> `dirs -v` (show directory stack)

    All plugins: platform 'all' unless platform-specific, version '1.0.0'.

    Update `src/plugins/index.ts`:
    - Import all 7 completion plugins from `./completions/`
    - Import all 8 utility plugins from `./utilities/`
    - Add all to BUNDLED_PLUGINS array (git alias plugin + 7 completion + 8 utility = 16 total)

    Create `tests/plugins/utilities.test.ts`:
    - Test each utility plugin has valid name and version
    - Test extract plugin has EXTRACTORS covering .tar.gz, .zip, .gz
    - Test copypath aliases differ by platform
    - Test web-search aliases contain URL patterns
    - Test BUNDLED_PLUGINS length is 16
  </action>
  <verify>
    <automated>cd /Users/tald/Projects/nesh/main && npx vitest run tests/plugins/utilities.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/plugins/utilities/ | wc -l` returns 8
    - `ls src/plugins/completions/ | wc -l` returns 7
    - `grep "BUNDLED_PLUGINS" src/plugins/index.ts` matches
    - `grep -c "import" src/plugins/index.ts` returns >= 16 (all plugin imports)
    - `grep "name: 'extract'" src/plugins/utilities/extract.ts` matches
    - `grep "name: 'sudo'" src/plugins/utilities/sudo.ts` matches
    - `grep "name: 'copypath'" src/plugins/utilities/copypath.ts` matches
    - `grep "pbcopy\\|xclip" src/plugins/utilities/copypath.ts` matches (cross-platform)
    - `npx vitest run tests/plugins/utilities.test.ts` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>8 utility plugins and 7 completion plugins bundled; BUNDLED_PLUGINS updated with all 16 plugins; cross-platform support via process.platform checks</done>
</task>

</tasks>

<verification>
```bash
cd /Users/tald/Projects/nesh/main
npx vitest run tests/completions/ tests/plugins/
npx tsc --noEmit
npm run build
```
All tests pass. TypeScript compiles clean. Build succeeds.
</verification>

<success_criteria>
- 20 commands have hand-crafted completion specs (git, docker, npm, yarn, pnpm, kubectl, ssh, aws, gcloud, az, terraform, helm, cargo, pip, python, node, make, systemctl, brew, apt)
- Git completions include dynamic generators for branches, remotes, tags, and filepaths template for git add
- 8 utility plugins ported: extract, sudo, copypath, encode64, urltools, jsontools, web-search, dirhistory
- All plugins registered in BUNDLED_PLUGINS
- Cross-platform support via process.platform checks
- All tests pass, tsc clean, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/09-completion-engine-utility-plugins/09-03-SUMMARY.md`
</output>
