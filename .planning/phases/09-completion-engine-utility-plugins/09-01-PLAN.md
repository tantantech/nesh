---
phase: 09-completion-engine-utility-plugins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/completions/types.ts
  - src/completions/cache.ts
  - src/completions/compgen.ts
  - src/completions/spec-parser.ts
  - tests/completions/types.test.ts
  - tests/completions/cache.test.ts
  - tests/completions/compgen.test.ts
  - tests/completions/spec-parser.test.ts
autonomous: true
requirements:
  - COMP-02
  - COMP-03
  - COMP-04

must_haves:
  truths:
    - "Completion type contracts exist for providers, contexts, results, and Fig-style specs"
    - "TTL cache stores and expires entries correctly"
    - "Compgen fallback returns command and file completions from bash subprocess"
    - "Spec parser walks a CompletionSpec tree and returns matching completions for a token path"
  artifacts:
    - path: "src/completions/types.ts"
      provides: "CompletionContext, CompletionResult, CompletionProvider, CompletionSpec, CompletionOption, CompletionArg, CompletionGenerator"
      exports: ["CompletionContext", "CompletionResult", "CompletionProvider", "CompletionSpec", "CompletionOption", "CompletionArg", "CompletionGenerator"]
    - path: "src/completions/cache.ts"
      provides: "TTL cache with get/set/clear"
      exports: ["createCompletionCache"]
    - path: "src/completions/compgen.ts"
      provides: "bash compgen subprocess fallback"
      exports: ["compgenComplete"]
    - path: "src/completions/spec-parser.ts"
      provides: "Fig-style spec tree walker"
      exports: ["resolveFromSpec"]
  key_links:
    - from: "src/completions/spec-parser.ts"
      to: "src/completions/types.ts"
      via: "imports CompletionSpec, CompletionArg, CompletionGenerator"
      pattern: "import.*from.*types"
    - from: "src/completions/compgen.ts"
      to: "bash subprocess"
      via: "execFile('bash', ['-c', ...])"
      pattern: "execFile.*bash"
---

<objective>
Create the completion engine foundation: type contracts, TTL cache, compgen fallback, and Fig-style spec parser as independent, testable modules.

Purpose: These four modules are the building blocks consumed by the completion engine dispatcher (Plan 02). Each is self-contained with no integration dependencies.
Output: 4 new modules in `src/completions/` with full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-completion-engine-utility-plugins/09-CONTEXT.md
@.planning/phases/09-completion-engine-utility-plugins/09-RESEARCH.md

@src/plugins/types.ts
@src/plugins/hooks.ts

<interfaces>
<!-- Existing plugin types that completion types must align with -->

From src/plugins/types.ts:
```typescript
export type HookHandler = (context: Readonly<HookContext>) => void | Promise<void>

export interface PluginManifest {
  readonly name: string
  readonly version: string
  readonly description: string
  readonly aliases?: Readonly<Record<string, string>>
  readonly dependencies?: readonly string[]
  readonly platform?: 'macos' | 'linux' | 'all'
  readonly permissions?: readonly string[]
  readonly hooks?: Readonly<Partial<Record<HookName, HookHandler>>>
  readonly init?: (context: Readonly<HookContext>) => Promise<void>
  readonly destroy?: (context: Readonly<HookContext>) => Promise<void>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Completion types, TTL cache, and compgen fallback</name>
  <files>src/completions/types.ts, src/completions/cache.ts, src/completions/compgen.ts, tests/completions/types.test.ts, tests/completions/cache.test.ts, tests/completions/compgen.test.ts</files>
  <read_first>src/plugins/types.ts, src/plugins/hooks.ts</read_first>
  <behavior>
    - types.test.ts: CompletionContext has fields: line, cursor, words, currentWord, commandName, cwd
    - types.test.ts: CompletionResult has fields: items (readonly string[]), prefix (optional string)
    - types.test.ts: CompletionSpec has fields: name, subcommands (optional Record), options (optional array), args (optional array)
    - types.test.ts: CompletionOption supports name as string or string[] (short/long aliases)
    - types.test.ts: CompletionArg supports template ('filepaths' | 'folders') and generators (CompletionGenerator[])
    - types.test.ts: CompletionGenerator is async function returning string[]
    - cache.test.ts: get() returns undefined for missing key
    - cache.test.ts: set() then get() returns stored value
    - cache.test.ts: get() returns undefined after TTL expires (use fake timers)
    - cache.test.ts: set() evicts oldest entry when maxSize exceeded
    - cache.test.ts: clear() removes all entries
    - compgen.test.ts: compgenComplete('command', 'gi') returns array of strings (mock execFile)
    - compgen.test.ts: compgenComplete('file', 'src/') returns array of strings (mock execFile)
    - compgen.test.ts: compgenComplete rejects unsafe input containing shell metacharacters (;|&`$)
    - compgen.test.ts: compgenComplete returns empty array on subprocess timeout
    - compgen.test.ts: compgenComplete returns empty array when bash not found
  </behavior>
  <action>
    Create `src/completions/types.ts` with all completion type contracts per D-02 through D-04, D-13 through D-17:
    - `CompletionContext`: `{ readonly line: string, readonly cursor: number, readonly words: readonly string[], readonly currentWord: string, readonly commandName: string, readonly cwd: string }`
    - `CompletionResult`: `{ readonly items: readonly string[], readonly prefix?: string }`
    - `CompletionProvider`: `(context: Readonly<CompletionContext>) => Promise<CompletionResult>`
    - `CompletionSpec`: `{ readonly name: string, readonly subcommands?: Readonly<Record<string, CompletionSpec>>, readonly options?: readonly CompletionOption[], readonly args?: readonly CompletionArg[] }`
    - `CompletionOption`: `{ readonly name: string | readonly string[], readonly description?: string, readonly args?: readonly CompletionArg[] }`
    - `CompletionArg`: `{ readonly name: string, readonly template?: 'filepaths' | 'folders', readonly generators?: readonly CompletionGenerator[] }`
    - `CompletionGenerator`: `(context: Readonly<CompletionContext>) => Promise<readonly string[]>`
    - Also export `parseCompletionContext(line: string): CompletionContext` helper that splits line into words, extracts currentWord (last token or empty string), commandName (first token), cursor = line.length, cwd = process.cwd().

    Create `src/completions/cache.ts` per D-06, D-35:
    - `createCompletionCache<T>(maxSize = 500)` returning `{ get(key): T | undefined, set(key, value, ttlMs): void, clear(): void }`
    - Uses `Map<string, { readonly value: T, readonly expiresAt: number }>` internally
    - `get()` checks `Date.now() > expiresAt` and deletes expired entries
    - `set()` evicts first key when at maxSize before inserting
    - No external dependencies

    Create `src/completions/compgen.ts` per D-19 through D-22:
    - `compgenComplete(type: 'command' | 'file', currentWord: string): Promise<readonly string[]>`
    - Reject unsafe input: check against `/[;&|` + "`" + `$(){}'"\\<>!#~]/` regex, return empty array
    - For 'command': run `bash -c "compgen -c -- \"${currentWord}\""` with 500ms timeout
    - For 'file': run `bash -c "compgen -f -- \"${currentWord}\""` with 500ms timeout
    - Use `promisify(execFile)` from `node:child_process`
    - On any error (timeout, bash not found, nonzero exit): return empty array, never throw
    - All imports use `.js` extension (ESM)
  </action>
  <verify>
    <automated>cd /Users/tald/Projects/nesh/main && npx vitest run tests/completions/cache.test.ts tests/completions/compgen.test.ts tests/completions/types.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CompletionContext" src/completions/types.ts` returns >= 1
    - `grep -c "CompletionSpec" src/completions/types.ts` returns >= 1
    - `grep -c "CompletionGenerator" src/completions/types.ts` returns >= 1
    - `grep -c "CompletionProvider" src/completions/types.ts` returns >= 1
    - `grep -c "parseCompletionContext" src/completions/types.ts` returns >= 1
    - `grep -c "createCompletionCache" src/completions/cache.ts` returns >= 1
    - `grep -c "compgenComplete" src/completions/compgen.ts` returns >= 1
    - `grep -c "UNSAFE_CHARS" src/completions/compgen.ts` returns >= 1
    - `grep "timeout.*500" src/completions/compgen.ts` matches (500ms timeout)
    - All test files pass: `npx vitest run tests/completions/` exits 0
  </acceptance_criteria>
  <done>All completion types exported, cache stores/expires/evicts correctly, compgen returns results for commands/files and safely rejects unsafe input</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fig-style spec parser</name>
  <files>src/completions/spec-parser.ts, tests/completions/spec-parser.test.ts</files>
  <read_first>src/completions/types.ts</read_first>
  <behavior>
    - resolveFromSpec with tokens ['checkout'] on git spec returns checkout subcommand's completions (branch generators)
    - resolveFromSpec with tokens ['branch', '-d'] returns branch names from generator for the -d option's arg
    - resolveFromSpec with unknown subcommand returns top-level subcommand names as completions
    - resolveFromSpec with empty tokens returns all top-level subcommand names
    - resolveFromSpec with option starting with '-' returns matching options from current spec level
    - resolveFromSpec invokes generators from CompletionArg when present
    - resolveFromSpec handles CompletionArg with template 'filepaths' by returning compgen file results
    - resolveFromSpec with deeply nested spec (3 levels) walks correctly
  </behavior>
  <action>
    Create `src/completions/spec-parser.ts` per D-18:
    - `resolveFromSpec(spec: CompletionSpec, tokens: readonly string[], currentWord: string, context: Readonly<CompletionContext>): Promise<CompletionResult>`
    - Walk the spec tree: for each token in tokens, if it matches a subcommand name, descend into that subcommand's spec. Stop when tokens are exhausted or no match.
    - At the final spec level, generate completions based on currentWord:
      - If currentWord starts with '-': filter options whose name (string or string[]) starts with currentWord
      - Otherwise: collect subcommand names that start with currentWord. If no subcommand matches and spec has args, invoke arg generators (with 1s timeout via Promise.race per D-05) and/or handle template ('filepaths' calls compgenComplete('file', currentWord))
    - For options with `args`, when the previous token matches an option name, complete the option's arg instead of the subcommand
    - Return `{ items: [...matchingCompletions], prefix: currentWord }`
    - All generators wrapped in try/catch -- on error return empty array
    - Use `Promise.race` with 1-second timeout for each generator call
    - Import types from `./types.js`, compgenComplete from `./compgen.js`
  </action>
  <verify>
    <automated>cd /Users/tald/Projects/nesh/main && npx vitest run tests/completions/spec-parser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "resolveFromSpec" src/completions/spec-parser.ts` returns >= 1
    - `grep "Promise.race" src/completions/spec-parser.ts` matches (timeout on generators)
    - `grep "compgenComplete" src/completions/spec-parser.ts` matches (file template handling)
    - All spec-parser tests pass: `npx vitest run tests/completions/spec-parser.test.ts` exits 0
  </acceptance_criteria>
  <done>Spec parser walks CompletionSpec trees, resolves subcommands/options/args with generator timeout, handles filepaths template via compgen fallback</done>
</task>

</tasks>

<verification>
```bash
cd /Users/tald/Projects/nesh/main
npx vitest run tests/completions/
npx tsc --noEmit
```
All completion foundation tests pass. TypeScript compiles without errors.
</verification>

<success_criteria>
- 4 new modules in src/completions/ (types, cache, compgen, spec-parser)
- All exports match the type contracts defined in decisions D-02 through D-22
- TTL cache correctly expires and evicts
- Compgen safely sanitizes input and handles timeout/missing bash
- Spec parser walks nested specs and invokes generators with timeout
- All tests pass, tsc --noEmit clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-completion-engine-utility-plugins/09-01-SUMMARY.md`
</output>
