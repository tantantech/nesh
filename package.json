{
  "name": "nesh",
  "version": "0.3.0",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "nesh": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsdown src/cli.ts --format esm --target node22 --out-dir dist && mv dist/cli.mjs dist/cli.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build && npm run test"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "keywords": [
    "shell",
    "terminal",
    "cli",
    "claude",
    "ai",
    "agent"
  ],
  "author": "tantan_tald",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/tantantech/nesh"
  },
  "description": "Nesh — an AI-native terminal shell powered by Claude",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.91",
    "@google/generative-ai": "^0.24.1",
    "marked": "^15.0.12",
    "marked-terminal": "^7.3.0",
    "openai": "^6.33.0",
    "picocolors": "^1.1.1"
  },
  "overrides": {
    "@anthropic-ai/sdk": ">=0.82.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsdown": "^0.21.7",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
